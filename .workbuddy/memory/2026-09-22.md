# 2026-09-22

- 文案修改：`src/themes/relaxed.ts` 「为人严肃」卡内容改为「我们受过严格的训练，无论多好笑呢，我们都不会笑」。
- 勇者大闯关彩蛋卡顺序调整：`src/components/MultiplayerMode.tsx` 新增 `bonusShown` 状态，`showingBonus = bonusIndex !== null && !bonusShown`，触发彩蛋（50% 概率，`drawAdventureBonus`）时彩蛋卡先弹出、其语音先播；关闭彩蛋后再依次展示抽中的常规卡并播对应语音。多轮（resultQueue > 1）与极速关闭（afterSpinAudio 等待转盘音余音）路径均保留。
- 魔鬼卡配乐随机化：`MultiplayerMode.tsx` 新增 `devilMusicCandidates`（adventure-devil1/2/3-music.mp3）+ `probeAudioExists`（Audio loadedmetadata 探测，Map 缓存）+ `pickDevilMusic`（洗牌随机挑一首，全缺失回退 adventure-devil-music.mp3）；`playNext` 改 async，配 `playSequence` 令牌防探测期间关闭弹窗的竞态。新音频放进 `public/audio` 即生效，无需改代码。天使卡配乐保持固定。
- IDE 源代码管理失灵（已修复）：根因是 `.git/refs/heads/main` 文件丢失（`HEAD` 仍指向它），git 无法识别仓库。通过 `logs/refs/heads/main` 取出最后一次 commit `7b8135cc50d9867fedb467c3552e424d282416b4`，先 `mkdir -p .git/refs/heads` 再重建 main 指针后，`git status` 退出码 0 不再报错。用户反馈同目录下其他项目正常也验证不是文件夹名问题。工作区目前干净，IDE 源代码管理应已恢复，需用户切回 Trae 确认。
- 彩蛋冷却：`adventureBonus.ts` 新增 `createAdventureBonusDrawer()`（`BONUS_COOLDOWN_DRAWS = 3`），触发彩蛋后接下来 3 次抽取必定不出；`MultiplayerMode.tsx` 用 `useRef` 持有抽选器（组件随 session key 重建时重置）。注意：用户已把 `drawAdventureBonus` 签名从 `(random = Math.random)` 函数参数改为数值参数 `(random = Math.random())`，wrapper 按数值签名写。新增 3 条冷却测试。
- 验证：`npx tsc -b --noEmit` 通过；`node --test`（逐文件传入，目录形式会因中文路径编码报错）45/47 通过，2 个失败为存量问题（`app-layout-source.test.ts` 期望旧版 header className、`header-menu-source.test.ts` 期望 `setThemesOpen((value) => !value)`，与本次改动文件无关）。
- 环境备注：本机终端（bash/PowerShell shim）对含「＆」等全角字符的路径传参给 node 时会乱码，跑测试需逐文件传参；项目当前不是 git 仓库，git 命令不可用。
